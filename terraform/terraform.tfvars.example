# Copy this file to terraform.tfvars and fill in your values

hcloud_token = "your-hetzner-api-token"

cluster_name = "kusama-validators"

# Hetzner locations for multi-geo (default: all 3)
# locations = ["fsn1", "nbg1", "hel1"]

# Server types (uncomment to override)
# control_plane_server_type = "cpx21"
# worker_server_type = "cpx41"

# Initial scale
# initial_workers_per_location = 1

# Security
# Only allow SSH/API from your IP (Recommended!)
# allowed_ips = ["203.0.113.5/32"]
